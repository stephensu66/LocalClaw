{
  "name": "openclaw-mvp",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "release:desktop:macos": "pnpm --filter @openclaw/desktop release:macos",
    "release:desktop:windows": "pnpm --filter @openclaw/desktop release:windows",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.6.3"
  }
}
